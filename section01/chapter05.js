// 1. Number Type
let num1 = 27;
let num2 = 1.5;
let num3 = -20;

// 무한대(Infinity)를 포함하는 변수 inf를 선언
let inf = Infinity;
// 음의 무한대(-Infinity)를 포함하는 변수 mInf를 선언
let mInf = -Infinity;
// NaN(Not a Number)을 포함하는 변수 nan을 선언
let nan = NaN;

// 2. String Type
let myName = "구준모";
let myLocation = "부산";
let introduce = myName + myLocation;

// 백틱(``) 에서는 문자열 안에 변수의 값을 동적으로 집어 넣을수 있다.
let introduceText = `${myName}는 ${myLocation}에 거주합니다`;

// 템플릿 리터럴 문법

// 3. Boolean Type
let isSwitchOn = true;
let isEmpty = false;

// 4. Null Type (아무것도 없다)
let empty = null;

// 5. Undefined Type
let none;
console.log(none);